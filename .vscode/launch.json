{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-safari",
            "request": "launch",
            "name": "Launch Safari against localhost",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}